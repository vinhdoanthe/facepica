<%= form_with(model: authorized_client, local: true) do |form| %>
  <% if authorized_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authorized_client.errors.count, "error") %> prohibited this authorized_client from being saved:</h2>

      <ul>
      <% authorized_client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_name %>
    <%= form.text_field :client_name %>
  </div>

  <div class="field">
    <%= form.label :client_secret %>
    <%= form.text_area :client_secret, :readonly => true %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_area :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
